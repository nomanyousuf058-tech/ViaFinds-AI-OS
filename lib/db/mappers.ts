import { ArticleRow, CategoryRow, ReviewRow } from './types'

export function rowToArticle(row: ArticleRow): ArticleRow {
  return row
}

export function rowToReview(row: ReviewRow): ReviewRow {
  return row
}

export function rowToCategory(row: CategoryRow): CategoryRow {
  return row
}
