import { BaseService } from '../BaseService';
import { ISearchRepository } from './interfaces';

export class SearchRepository extends BaseService implements ISearchRepository {
  constructor() {
    super('SearchRepository');
  }

  // Placeholder methods for data access
}
